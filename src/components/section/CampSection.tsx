const CampSection = () => {

    return (
        <div>
            {
                //loop through the campData array and display the camp name
                //campData key
                // campData.map((camp) => {
            }
        </div>
    );
};

export default CampSection;
